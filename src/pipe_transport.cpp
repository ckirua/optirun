#include "optirun/transport.hpp"

namespace optirun::detail { // The concrete POSIX descriptor transport lives
                            // with Runtime because it owns
// the child lifetime. This translation unit reserves the transport boundary for
} // namespace optirun::detail
